Original prompt: 暗い画面でやってるとマジで青と緑の誓いがわからん

- 2026-03-20: Adjusted the global block palette to increase blue/green separation.
- 2026-03-20: Switched PURIFY mode to a higher-contrast color subset so it avoids green entirely.
- 2026-03-20: Added subtle shape cues to blue and green blocks in the renderer and rule-card swatches.
- 2026-03-20: Verified with `npm test`, `npm run build`, `npx cap sync`, plus fresh screenshots in `.checks/rules-color-contrast.png` and `.checks/classic-color-contrast.png`.
- 2026-03-20: Reworked interstitial handling so native ads keep a single init path, wait for late loads on eligible game overs, and automatically re-prime after dismiss/fail events.
- 2026-03-20: Delayed game-over interstitial requests slightly and gated them to the actual game-over screen so ads do not pop after the player has already left the result screen.
- 2026-03-20: Added `ads.test.ts` coverage for session gating, late-load display, and cancelled game-over ads; verified with `npm test`, `npm run build`, and `npx cap sync android`.
- 2026-03-21: Added native-bridge ad tests for Android (`NEXT_PUBLIC_USE_NATIVE_ANDROID_ADS=true`) covering initialize/prepare/show and debug-state refresh.
- 2026-03-21: Verified `npm test` (74 passed), `npm run build`, and `scripts/build_android_release.ps1 -AdMode test`, producing `meteor-crush-v1.0-testads-vc11.apk` and `.aab`.
- 2026-03-21: Attempted emulator-side ad verification via `adb`, but only `devices/get-state` responded; `adb shell`/`logcat` calls hung in this environment, so real ad rendering could not be confirmed from the terminal.
- 2026-03-21: Hardened `build_android_release.ps1` to clean `.next` and Android build intermediates before building, fixing OneDrive reparse-point/EPERM build failures.
- 2026-03-21: Rebuilt the test-ads Android release successfully after the clean-step fix, producing `meteor-crush-v1.0-testads-vc13.apk` and `.aab`.
- 2026-03-21: Split ad configuration into three concepts: live ads, test-ad debug builds, and internal-test builds that keep test ads but preserve production-style gating.
- 2026-03-21: Added coverage in `ads.test.ts` for the internal-test case (`NEXT_PUBLIC_USE_TEST_AD_UNITS=true` without debug bypass), bringing the suite to 75 passing tests.
- 2026-03-21: Built the internal-test package successfully via `build_android_release.ps1 -AdMode internal`, producing `meteor-crush-v1.0-testapp-vc14.apk` and `.aab`.
- 2026-03-22: Added web SEO foundations: indexable landing page at `/`, richer metadata/open graph/twitter tags, canonical URLs, structured data, `robots.txt`, and `sitemap.xml`.
- 2026-03-22: Verified the SEO build with `npm run build`; static output now includes `/robots.txt` and `/sitemap.xml`.
- 2026-03-22: Deployed the SEO update to Cloudflare Pages. `master.meteor-crush.pages.dev` returns the new sitemap/robots immediately; the apex `meteor-crush.pages.dev` may lag briefly in propagation.
- 2026-03-22: Reworked the language system into a shared client-side provider with persisted selection, instead of fixed `navigator.language` snapshots.
- 2026-03-22: Added a language picker to the landing page and game settings, and localized the landing page plus privacy policy in both Japanese and English.
- 2026-03-22: Added `i18n.test.ts`, bringing the suite to 79 passing tests, then deployed the multilingual web update to production.
- 2026-03-23: Added a dedicated Web AdSense game-over card, reusing the same ad-free/session gating as mobile so browser ads now appear on eligible game overs instead of relying on passive page placement.
- 2026-03-23: Verified the Web game-over ad flow with `npm test` (81 passed), `npm run build`, and Playwright against a local dev server with a temporary `NEXT_PUBLIC_ADSENSE_GAME_OVER_SLOT`; the third game over showed the injected ad card in `.playwright-cli/page-2026-03-23T13-28-39-922Z.png`.
- 2026-03-26: Reworked Classic away from the standard tetromino roster into an original meteor-fragment set (`Spark`, `Bend`, `Slab`, `Arc`, `Vane`, `Wave`, `Nova`, plus `Comet`) to reduce visual/mechanical overlap with Tetris.
- 2026-03-26: Rebuilt Purify around 3-cell triad shards and connected-cluster cleansing; same-color groups now clear only when a fixed core is inside a cluster of 5 or more cells, which moves the mode away from Dr. Mario's pill + line-clear structure.
- 2026-03-26: Updated rule cards, landing copy, and metadata to describe the original fragment roster and triad-shard Purify rules so the web build consistently presents the redesign.
- 2026-03-26: Added regression coverage for the custom fragment roster and new Purify cluster rules in `PieceRoster.test.ts`, `PurifyMode.test.ts`, `CoreMatchResolver.test.ts`, and `GameEngine.test.ts`.
- 2026-03-26: Verified the redesign with `npm test` (82 passed), `npm run build`, and Playwright spot checks on a local static server; updated rule text/screens are captured in `.playwright-cli/page-2026-03-26T14-33-20-793Z.png` and `.playwright-cli/page-2026-03-26T14-34-09-520Z.png`.
- 2026-03-27: Built the current web preview with `npm run build` and deployed it to Cloudflare Pages as the `redesign-preview` branch for testing.
- 2026-03-27: Verified both preview URLs return HTTP 200: `https://redesign-preview.meteor-crush.pages.dev` and the deployment-specific `https://73610239.meteor-crush.pages.dev`.
- 2026-03-27: Added a new 3x2 support-weight rescue piece for Classic/Bomber. When it spawns and lands, it packs solid steel blocks into the empty cells directly beneath its footprint to smooth over narrow pits.
- 2026-03-27: Wired support-weight spawning through a shared `SupportWeightSpawner` so it only appears after a few pieces and only when the stack is getting tall, holey, or rough.
- 2026-03-27: Updated the renderer and rule cards so the support weight reads as a heavy steel ballast block, not a normal fragment.
- 2026-03-27: Added regression coverage for support-weight spawning and landing behavior, plus a safer wall-bound assertion that checks occupied cells instead of the raw piece origin.
- 2026-03-27: Verified the change with `npm test` (86 passed), `npm run build`, and a local dev-browser check. The forced support-weight scenario screenshots are in `.playwright-cli/page-2026-03-26T15-25-27-144Z.png` and `.playwright-cli/page-2026-03-26T15-25-42-078Z.png`.
- 2026-03-27: Drafted the fourth-mode concept as `ARMORY` in `docs/mode4-armory-spec.md`, including the core loop, four starter weapons, readability constraints, chain rules, and a phased implementation plan.
- 2026-03-27: Implemented `ARMORY` as a full fourth mode. Falling pieces now carry 3x3 weapon-fragment metadata, completed weapon images trigger themed attacks (`missile`, `bomb`, `tub`, `pan`), and the engine resolves chained weapon activations with dedicated scoring and chain text.
- 2026-03-27: Added ARMORY-specific data/modeling (`ArmoryData`, `ArmoryResolver`, `ArmoryMode`) plus renderer support so board cells, active pieces, and next/hold previews all draw as illustrated fragment tiles instead of normal meteors.
- 2026-03-27: Updated the game shell, rule sheet, landing copy, and metadata so ARMORY is selectable from the main mode list and consistently described across the web build.
- 2026-03-27: Added regression coverage in `ArmoryResolver.test.ts` and `GameEngine.test.ts`, bringing the suite to `npm test` 91 passing tests; `npm run build` also succeeded.
- 2026-03-27: Verified ARMORY in a local dev browser via the debug helpers on `127.0.0.1:3100/game`, forcing a bomb recipe and confirming the `BOMB BURST` attack/score flow. Screenshots: `.playwright-cli/armory-bomb-seeded.png` and `.playwright-cli/armory-bomb-resolved.png`.
- 2026-03-27: Simplified ARMORY after playtesting. Weapon activation now uses a clear `2x2 same-weapon plate` rule instead of exact 3x3 image reconstruction, and the piece pool was narrowed to single-weapon plate sets so the mode is easier to read and build around.
- 2026-03-27: Reworked ARMORY rendering and UI to use full per-cell weapon icons plus a persistent HUD hint (`2x2 SAME WEAPON`) instead of tiny image slices. Updated the rule sheet, landing copy, and metadata to match the new rule.
- 2026-03-27: Re-verified the redesigned ARMORY with `npm test` (91 passed), `npm run build`, and a local dev-browser bomb trigger check. Updated screenshots: `.playwright-cli/armory-2x2-seeded.png` and `.playwright-cli/armory-2x2-resolved.png`.
- 2026-03-28: Reworked BOMBER away from row-based ignition. Bombs now only ignite when touching a fire block, completed rows do nothing in this mode, and chained blasts continue when explosions hit other bombs.
- 2026-03-28: Added two Bomber payload types: `thunder` bombs explode in an X pattern and `cluster` bombs explode in a plus pattern. Falling Bomber pieces now randomly carry fire blocks, thunder bombs, or cluster bombs instead of the old generic/mega-bomb setup.
- 2026-03-28: Updated Bomber rendering, HUD hints, rule-card copy, landing copy, and debug helpers so the new ignition rule is visible and testable in the web build.
- 2026-03-28: Added regression coverage for Bomber ignition and blast shapes in `BombResolver.test.ts` and `GameEngine.test.ts`. Verified with `npm test` (92 passed), `npm run build`, and a local dev-browser ignition scenario. Screenshots: `.playwright-cli/bomber-ignite-seeded-v2.png` and `.playwright-cli/bomber-ignite-resolved-v2.png`.
- 2026-03-29: Corrected the Anchor/support-weight behavior so it no longer creates new filler blocks. It now compresses the existing blocks in the covered columns downward, closing holes by gravity under the landed weight.
- 2026-03-29: Updated the support-weight regression test, rule-sheet copy, and dev debug scenario to match the new “press the stack down” behavior.
- 2026-03-29: Re-verified the Anchor change with `npm test` (92 passed), `npm run build`, a required Playwright smoke run, and a local dev-browser support-weight scenario. Screenshots: `.playwright-cli/support-weight-compress-seeded.png` and `.playwright-cli/support-weight-compress-resolved.png`; board dump: `.playwright-cli/support-weight-compress-state.json`.
- 2026-03-29: Built a fresh Android internal-test package with the Anchor fix via `scripts/build_android_release.ps1 -AdMode internal`. Output files: `C:\Users\PC\Downloads\meteor-crush-v1.0-testapp-vc20.apk` and `.aab`; Android `versionCode` is now 20.
- 2026-03-29: Reworked BOMBER again. `thunder` bombs now explode diagonally all the way to the board edge, `cluster` bombs explode orthogonally to the edge, and explosions can chain into other bombs they touch.
- 2026-03-29: Restored the smaller legacy `normal` bomb in Bomber alongside fire/thunder/cluster payloads, extended explosion visuals to 1000 ms, and removed support-weight/Anchor spawning from Bomber entirely.
- 2026-03-29: Reinterpreted Classic's Anchor to affect whole columns: when the 3x2 weight lands, every occupied cell in the covered columns is repacked downward to the lowest available spaces instead of only filling beneath the weight.
- 2026-03-29: Removed the easter-egg progression from the active game flow, made PURIFY available by default from boot, set PURIFY as the default selected/ranking mode, and removed the hidden-mode/ad-free reward behavior.
- 2026-03-29: Updated Bomber/Purify/Classic copy across the rule sheet, landing page, metadata, and debug helpers so the web build matches the new rules and no longer advertises hidden content.
- 2026-03-29: Verified the refreshed mode rules with `npm test` (92 passed), `npm run build`, required Playwright smoke coverage, and local dev-browser state checks on `127.0.0.1:3100/game`. Key artifacts: `.playwright-cli/bomber-edge-seeded-v3.png`, `.playwright-cli/bomber-edge-resolved-v3.png`, `.playwright-cli/anchor-column-seeded-v2.png`, `.playwright-cli/anchor-column-resolved-v2.png`, and `.playwright-cli/mode-refresh-state.json`.
- 2026-03-29: Built a fresh Android internal-test package after the Bomber/Classic/Purify mode refresh via `scripts/build_android_release.ps1 -AdMode internal`. Output files: `C:\Users\PC\Downloads\meteor-crush-v1.0-testapp-vc21.apk` and `.aab`; Android `versionCode` is now 21.
- 2026-03-30: Tuned BOMBER difficulty upward by cutting fire-block spawn odds from 10% to 3%, then restored a payoff route by fusing any 2x2 square of the same bomb kind into a larger mega bomb.
- 2026-03-30: Reworked bomb-chain handling so fused mega bombs resolve as a single explosion source instead of four separate triggers; mega bombs now use the existing oversized visual and a wider effective blast.
- 2026-03-30: Expanded ARMORY from 4 weapon families to 7 by adding `katana`, `sword`, and `spear` alongside the existing missile/bomb/tub/pan set. Added new colors, board effects, renderer icons, and rule/landing copy for the larger weapon roster.
- 2026-03-30: Added regression coverage for mega-bomb ignition/explosion behavior plus a new ARMORY spear-effect test. `npm test` now passes with 96 tests, and `npm run build` succeeds.
- 2026-03-30: Verified the balance pass with the required Playwright client plus local headed-state screenshots on `127.0.0.1:3100/game`. Key artifacts: `.playwright-cli/bomber-mega-seeded-v5.png`, `.playwright-cli/bomber-mega-resolved-v5.png`, `.playwright-cli/armory-spear-seeded-v1.png`, `.playwright-cli/armory-spear-resolved-v1.png`, `.playwright-cli/mode-balance-smoke/`, `.playwright-cli/mode-balance-state.json`, and `.playwright-cli/bomber-mega-state-v5.json`.
- 2026-03-30: Added a second Purify clear path for non-core cleanup. Straight horizontal or vertical lines of 4+ same-color normal blocks now clear even without a core, while the original 5+ connected core-cluster rule remains unchanged for cleansing cores.
- 2026-03-30: Updated Purify rule text, landing copy, i18n mode summaries, metadata, and debug helpers so the new `4-line cleanup + 5+ core cluster` split is visible across the web build.
- 2026-03-30: Added Purify regression coverage for horizontal/vertical line clears and a GameEngine integration test for non-core Purify crushes. `npm test` now passes with 99 tests, and `npm run build` succeeds.
- 2026-03-30: Verified the Purify line-clear update with the required Playwright client plus a local dev-browser seeded scenario on `127.0.0.1:3100/game`. Key artifacts: `.playwright-cli/purify-line-smoke/`, `.playwright-cli/purify-line-seeded-v1.png`, `.playwright-cli/purify-line-resolved-v1.png`, and `.playwright-cli/purify-line-state-v1.json`.
- 2026-03-30: Built a fresh Android internal-test package after the Purify line-clear update via `scripts/build_android_release.ps1 -AdMode internal`. Output files: `C:\Users\PC\Downloads\meteor-crush-v1.0-testapp-vc22.apk` and `.aab`; Android `versionCode` is now 22.
- 2026-03-30: Deployed the latest web build to Cloudflare Pages production (`main`) via `wrangler pages deploy out --project-name meteor-crush --branch main`. Deployment URL: `https://ff6f5e5e.meteor-crush.pages.dev`; both that URL and `https://meteor-crush.pages.dev` returned HTTP 200 immediately after deploy.
- 2026-03-30: Rebalanced ARMORY upward by mixing non-weapon junk pieces into the ARMORY piece pool. Junk cells never count toward 2x2 weapon triggers, so the mode now asks the player to carve around scrap instead of chaining pure weapon plates constantly.
- 2026-03-30: Widened every ARMORY attack to compensate for the junkier pool. Missile now fires a 4-column barrage, bomb bursts much farther out, tub hits a larger drop zone, pan sweeps four full-width rows, katana throws four diagonal slashes, sword cleaves a 3-column vertical lane, and spear thrusts forward across three rows.
- 2026-03-30: Updated ARMORY rule-card copy, landing copy, SEO/game metadata, i18n mode text, and added a new dev debug helper (`loadArmoryJunkScenario`) so the junk-mixed version is visible across the web build and easy to seed in-browser.
- 2026-03-30: Added ARMORY regression coverage for junk-not-counting plus the widened bomb/missile/sword/spear shapes. `npm test` now passes with 101 tests, and `npm run build` succeeds.
- 2026-03-30: Verified the ARMORY junk pass with the required Playwright client smoke run plus a seeded local browser scenario on `127.0.0.1:3100/game`. Key artifacts: `.playwright-cli/armory-junk-smoke/shot-0.png`, `.playwright-cli/armory-junk-seeded-v1.png`, `.playwright-cli/armory-junk-resolved-v1.png`, and `.playwright-cli/armory-junk-state-v1.json` (no console/page errors).
- 2026-03-30: Increased ARMORY junk frequency again by weighting the junk pool twice in the bag (`ARMORY_PIECES = weapons + junk + junk`). This keeps the mode harder even after the widened weapon attacks, without adding special-case spawn logic.
- 2026-03-30: Added `ArmoryData.test.ts` to lock the new pool weighting, bringing the suite to `npm test` 102 passing tests; `npm run build` also succeeds.
- 2026-03-30: Re-ran the required Playwright client smoke plus a seeded ARMORY junk screenshot after the weighting change. Useful artifacts: `.playwright-cli/armory-weight-smoke/shot-0.png`, `.playwright-cli/armory-junk-weight-seeded-v1.png`, and `.playwright-cli/armory-junk-weight-state-v1.json` (no console/page errors in the seeded run).
- 2026-03-30: Built a fresh Android internal-test package after doubling the ARMORY junk weighting via `scripts/build_android_release.ps1 -AdMode internal`. Output files: `C:\Users\PC\Downloads\meteor-crush-v1.0-testapp-vc24.apk` and `.aab`; Android `versionCode` is now 24.
- 2026-04-02: Investigated a report that placed blocks sometimes vanished after `DROP` or ordinary movement. The most concrete bug found was ARMORY overlap resolution: a 2x3 or 3x2 slab of the same weapon was being counted as multiple overlapping `2x2` triggers in one resolve pass, which caused extra attacks and made freshly placed blocks appear to disappear.
- 2026-04-02: Fixed `ArmoryResolver.findMatches()` so once a `2x2` weapon plate is consumed in a resolve pass, those four cells cannot be reused by another overlapping trigger that same frame. Added a regression test in `ArmoryResolver.test.ts` for the overlapping-slab case. `npm test` now passes with 103 tests, and `npm run build` succeeds.
- 2026-04-02: Added a new dev debug helper (`loadArmoryOverlapScenario`) and verified the fix in a local browser on `127.0.0.1:3100/game`. The seeded `2x3` bomb slab now resolves to exactly one weapon fire (`weapons: 1`) instead of two. Key artifacts: `.playwright-cli/armory-overlap-smoke/shot-0.png`, `.playwright-cli/armory-overlap-seeded-v1.png`, `.playwright-cli/armory-overlap-resolved-v1.png`, and `.playwright-cli/armory-overlap-state-v1.json`.
- 2026-04-02: Traced a second silent-disappear path in `CLASSIC`: if a rotated piece was still partly above the board (`y < 0`) when it locked, `lockPiece()` used to write only the in-bounds cells and silently drop the rest. This especially showed up after `DROP` near the top of the stack.
- 2026-04-02: Fixed `GameEngine.lockPiece()` so any lock attempt with occupied cells above the board now ends the run immediately instead of partially placing the piece. Added a regression test in `GameEngine.test.ts` for the overflowing `Nova` case, bringing the suite to `npm test` 104 passing tests; `npm run build` also succeeds.
- 2026-04-02: Added a `loadClassicOverflowScenario` debug helper and verified the Classic overflow fix in a local browser on `127.0.0.1:3100/game`. Immediate lock from a seeded `Nova` at `y = -1` now produces `GAME OVER` with an unchanged empty board rather than a partially placed piece. Key artifacts: `.playwright-cli/classic-overflow-smoke/shot-0.png`, `.playwright-cli/classic-overflow-immediate-v1.png`, and `.playwright-cli/classic-overflow-immediate-state-v1.json`.
- 2026-04-11: Rebalanced ARMORY harder again. The junk pool is now weighted three times in the ARMORY bag, and the previously widened weapon effects were pulled back one step so stray completed plates do not wipe the board as easily.
- 2026-04-11: Generalized the partial-lock guard in `GameEngine.lockPiece()` so any occupied cell outside the board bounds ends the run instead of silently writing only the in-bounds cells. Added side-wall overflow coverage in addition to the existing top-overflow regression.
- 2026-04-11: Verified the ARMORY/Classic hardening with `npm test` (105 passed), `npm run build`, the required Playwright client run, and local seeded browser checks on `127.0.0.1:3100/game`. Key artifacts: `.playwright-cli/armory-hardening-smoke/shot-0.png`, `.playwright-cli/armory-hardening-junk-v1.png`, `.playwright-cli/armory-hardening-resolved-v1.png`, `.playwright-cli/classic-overflow-immediate-v2.png`, `.playwright-cli/classic-overflow-immediate-state-v2.json`, and `.playwright-cli/armory-classic-hardening-state-v2.json`.
- 2026-04-11: Added ARMORY six-cell overdrive and weapon-hit chain rules. A same-weapon 2x3 or 3x2 now fires as `OVERDRIVE` with stronger attack range/visual force, and any weapon cell hit by another weapon attack can chain-trigger from a single cell.
- 2026-04-11: Reduced ARMORY weapon frequency to compensate for 1-cell chain triggers by weighting junk pieces six times in the ARMORY bag. Updated ARMORY HUD/rules/landing/SEO copy to describe `2x2 / 6 OVERDRIVE` and `1 weapon cell to chain`.
- 2026-04-11: Aligned ARMORY chain call text with other modes by routing multi-weapon chains through `ChainTextResolver` (`Double Crush`, `Triple Crush`, etc.) instead of `3X ARMORY`.
- 2026-04-11: Verified the overdrive/chain pass with `npm test` (108 passed), `npm run build`, the required Playwright client run, and seeded browser checks. Key artifacts: `.playwright-cli/armory-chain-call-smoke/shot-0.png`, `.playwright-cli/armory-overdrive-chain-seeded-v2.png`, `.playwright-cli/armory-overdrive-chain-resolved-v2.png`, and `.playwright-cli/armory-overdrive-chain-state-v2.json`.
- 2026-04-11: Changed shared chain scoring to double every chain step (`1x`, `2x`, `4x`, `8x`, ...). This now applies through the shared Bomber/Purify/Armory score calculators, and ARMORY same-wave weapon chains use the number of chained weapons for the multiplier.
- 2026-04-11: Added `ScoreCalculator.test.ts` coverage for doubled chain multipliers across Bomber, Purify, and Armory, plus an engine regression asserting a 2-weapon ARMORY chain scores at `2x`.
- 2026-04-11: Verified doubled chain scoring with `npm test` (110 passed), `npm run build`, the required Playwright client run, and a seeded ARMORY 3-chain browser check. Key artifacts: `.playwright-cli/chain-score-smoke/shot-0.png`, `.playwright-cli/chain-score-armory-resolved-v1.png`, and `.playwright-cli/chain-score-armory-state-v1.json` (`Triple Crush`, score `5208`).
- 2026-04-24: Removed the remaining ARMORY-only chain-call branch in `GameEngine`. Chain presentation is now shared through `ChainTextResolver`, so Bomber and Armory both use the same resolver path for `Double Crush` / `Triple Crush` style calls.
- 2026-04-24: Added `ChainTextResolver.test.ts` to lock the new shared chain-presentation helpers, and updated `GameEngine.test.ts` so ARMORY chain call expectations still pass through the common path.
- 2026-04-24: Verified the chain-expression fix with `npm test` (112 passed), `npm run build`, the required Playwright client run, and a seeded browser check. Key artifacts: `.playwright-cli/armory-chain-common-smoke/shot-0.png`, `.playwright-cli/armory-chain-common-resolved-v1.png`, and `.playwright-cli/armory-chain-common-state-v1.json` (`Triple Crush`, score `5208`).
- 2026-05-01: Investigated a renewed `CLASSIC` report that blocks were still “disappearing.” Browser-side invariant probes over repeated move/rotate/hard-drop cycles did not reproduce any board-cell loss, which pointed away from lock logic and toward a render/perception mismatch.
- 2026-05-01: Added browser-debug state output in `GameCanvas`: `window.render_game_to_text()` plus `window.__meteorCrushDebug.getState()`. The payload now reports current mode/state, active piece, ghost, next/hold, and all occupied board cells so Playwright runs can compare what the engine thinks exists against what is on screen.
- 2026-05-01: Changed the renderer to draw all 22 board rows and mark rows `0-1` as a visible `SPAWN` warning zone instead of hiding them entirely. This aligns visible playfield state with the rows that collision, spawn, line clear, and top-out logic already use, removing the invisible-row mismatch that made pieces look clipped or “gone” near the ceiling.
- 2026-05-01: Fixed a separate renderer bug in `drawGameOver()`: the crumble animation no longer mutates `Board` data while drawing. It now tracks dissolved rows internally and emits particles once per row without destroying live board cells from the render layer.
- 2026-05-01: Verified with `npm test` (112 passed), `npm run build`, the required Playwright client run, and paused browser screenshots/state dumps. Useful artifacts: `.playwright-cli/warning-rows-smoke/shot-0.png`, `.playwright-cli/warning-rows-smoke/state-0.json`, `.playwright-cli/classic-warning-shot.png`, and `.playwright-cli/classic-warning-state.json`.
- 2026-05-01: Reworked the main game loop in `GameCanvas` to use a `60 Hz` fixed timestep (`16.67 ms`) for all engine updates, with rendering left variable-rate. Large browser frame gaps are now clamped/caught up in bounded fixed steps instead of feeding raw long deltas into `engine.tick()`.
- 2026-05-01: Added a real deterministic `window.advanceTime(ms)` hook on top of the fixed-step loop. Browser checks now can advance the game by exact simulation time rather than waiting on wall-clock frames.
- 2026-05-01: Verified the fixed-step change with `npm test` (112 passed), `npm run build`, the required Playwright client run, and a manual browser determinism check where Classic `Spark` advanced from `y=0` to `y=1` after `advanceTime(1000)`. Useful artifacts: `.playwright-cli/fixed-step-smoke-v3/shot-0.png`, `.playwright-cli/fixed-step-smoke-v3/state-0.json`, `.playwright-cli/fixed-step-determinism-v3/after-1000ms.png`, and `.playwright-cli/fixed-step-determinism-v3/state.json`.
- 2026-05-01: Deployed the fixed-step web build to Cloudflare Pages production via `wrangler pages deploy out --project-name meteor-crush --branch main`. Deployment URL: `https://48364c51.meteor-crush.pages.dev`; both it and `https://meteor-crush.pages.dev` returned HTTP 200 immediately after deploy.
- 2026-05-01: Built a fresh Android internal-test package after the loop fix via `scripts/build_android_release.ps1 -AdMode internal`. Output files: `C:\Users\PC\Downloads\meteor-crush-v1.0-testapp-vc27.apk` and `.aab`; Android `versionCode` is now 27.
- 2026-05-01: Tightened Bomber difficulty again by reducing bomb payload frequency while leaving fire blocks unchanged. `NORMAL_BOMB_PROBABILITY`, `THUNDER_BOMB_PROBABILITY`, and `CLUSTER_BOMB_PROBABILITY` are now `0.05` each (down from `0.08`), while `FIRE_BLOCK_PROBABILITY` stays at `0.03`.
- 2026-05-01: Verified the Bomber spawn-rate tweak with `npm test` (112 passed), `npm run build`, and the required Playwright client run in Bomber mode. Useful artifacts: `.playwright-cli/bomber-probability-smoke-v2/shot-0.png` and `.playwright-cli/bomber-probability-smoke-v2/state-0.json`.
- 2026-05-24: Fixed a `HOLD` re-entry bug in `GameEngine`. Swapping in a held piece now uses the same spawn activation path as normal pieces, so blocked hold swaps top out immediately instead of leaving an invalid overlapping piece active.
- 2026-05-24: Added a `GameEngine.test.ts` regression for the blocked hold-swap case. `npm test` now passes with 113 tests, and `npm run build` succeeds.
- 2026-05-24: Deployed the updated web export to Cloudflare Pages production via `npx wrangler pages deploy out --project-name meteor-crush --branch main`. Deployment URL: `https://e47d99c4.meteor-crush.pages.dev`; both it and `https://meteor-crush.pages.dev` returned HTTP 200 immediately after deploy.
- 2026-05-25: Fixed a `CLASSIC` Anchor/support-weight presentation bug in `GameEngine.applySupportWeight()`. The landed 3x2 Anchor plate now stays intact at its lock position while the pre-existing stack in those columns is compressed downward, so the weight no longer appears to lose blocks when it lands.
- 2026-05-25: Updated the support-weight regression in `GameEngine.test.ts` to lock the intact-plate behavior. Verified with `npm test` (113 passed), `npm run build`, and a local browser debug scenario; artifacts: `output/anchor-verify/anchor-after.png` and `output/anchor-verify/anchor-after-state.json`.
- 2026-05-25: Deployed the Anchor fix to Cloudflare Pages production via `npx wrangler pages deploy out --project-name meteor-crush --branch main`. Deployment URL: `https://b5cf2968.meteor-crush.pages.dev`; both it and `https://meteor-crush.pages.dev` returned HTTP 200 immediately after deploy.
- 2026-05-26: Fixed a debug-state mismatch in `GameCanvas`. `createGameSnapshot()` now derives `mode` from `engine.mode.type` instead of stale UI state, and the dev scenario loaders also sync `selectedModeRef` when forcing Classic/Bomber/Armory/Purify boards.
- 2026-05-26: Added `GameCanvas.test.ts` regression coverage so Bomber and Armory snapshots report the engine mode directly. `npm test` now passes with 115 tests, and `npm run build` succeeds.
- 2026-05-26: Re-ran a local browser bug sweep plus the required Playwright client smoke. Fresh artifacts are under `output/bug-sweep-20260526/`, `output/bug-sweep-20260526-postfix/`, and `output/post-fix-smoke-20260526/`; the post-fix snapshots now report `mode: "bomber"` / `mode: "armory"` correctly.
- 2026-05-26: Deployed the updated web build to Cloudflare Pages production via `npx wrangler pages deploy out --project-name meteor-crush --branch main`. Deployment URL: `https://ba59741a.meteor-crush.pages.dev`; both it and `https://meteor-crush.pages.dev` returned HTTP 200 immediately after deploy.
- 2026-05-27: Fixed a stale-piece cleanup bug in `GameEngine`. Once a piece locks, or a spawn/top-out is rejected, `currentPiece` is now cleared before game-over handling so the renderer does not keep drawing a phantom active piece on top of the settled board.
- 2026-05-27: Added `GameEngine.test.ts` regressions for overflow top-out, blocked hold-swap top-out, and legal top-row top-out cleanup. `npm test` now passes with 116 tests, `npm run build` succeeds, and a browser-side debug check confirms the overflow scenario ends with `currentPiece: null`.
- 2026-05-27: Deployed the stale-piece fix to Cloudflare Pages production via `npx wrangler pages deploy out --project-name meteor-crush --branch main`. Deployment URL: `https://f7ffb6fd.meteor-crush.pages.dev`; both it and `https://meteor-crush.pages.dev` returned HTTP 200 immediately after deploy.
- TODO: If we want even more distance later, revisit mode naming/visual motifs so the legal separation is reinforced by branding as well as mechanics.
